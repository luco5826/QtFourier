#ifndef SETTINGS_HPP
#define SETTINGS_HPP

/**
 * Values used to draw the graph, edited in
 * the SettingsWidget
 */
struct Settings
{
    double refreshMs;
    double scaleFactor;
    double stretchFactor;
};

#endif